export type PrintableInvoiceData = {  id: string;  customerName: string;  phone?: string;  address?: string;  total: number;  paid: number;  receivedDate: string | Date;  deliveryDate: string | Date;  paymentDate?: string | Date;  notes?: string;};export function formatCurrency(value: number): string {  if (!Number.isFinite(value)) {    return '—';  }  try {    return new Intl.NumberFormat('en-US', {      style: 'currency',      currency: 'IQD',      maximumFractionDigits: 0,    }).format(value);  } catch {    return `${value} IQD`;  }}export function formatDate(dateLike: string | Date | undefined): string {  if (!dateLike) return '';  const date = typeof dateLike === 'string' ? new Date(dateLike) : dateLike;  if (Number.isNaN(date.getTime())) return '';  try {    return new Intl.DateTimeFormat('en-GB', {      year: 'numeric',      month: '2-digit',      day: '2-digit',    }).format(date);  } catch {    return date.toISOString().slice(0, 10);  }}type PrintableInvoiceProps = {  invoice: PrintableInvoiceData;};export function PrintableInvoice({ invoice }: PrintableInvoiceProps) {  const remaining = Math.max((invoice.total || 0) - (invoice.paid || 0), 0);  const currentDate = (() => {    try {      return new Intl.DateTimeFormat('en-GB', {        year: 'numeric',        month: 'long',        day: 'numeric',      }).format(new Date());    } catch {      return new Date().toISOString().slice(0, 10);    }  })();  const formatShortCurrency = (value: number): string => {    if (!Number.isFinite(value)) return '—';    try {      return new Intl.NumberFormat('en-US', { maximumFractionDigits: 0 }).format(value) + ' ?.?';    } catch {      return `${value} د.ع`;    }  };  return (    <div dir="rtl" style={{       fontFamily: 'Tajawal, system-ui, -apple-system, Segoe UI, Roboto, Arial',       width: '100%',       margin: '0 auto',       background: '#ffffff',       color: '#13312A',       pageBreakInside: 'avoid',      overflow: 'hidden'    }}>      {/* Local print styles for button visibility and optimized layout */}      <style>{`        @media print {           .print-btn { display: none !important; }          html, body {             background: #ffffff;             margin: 0;             padding: 0;             height: 100vh;            overflow: hidden;          }          * {             -webkit-print-color-adjust: exact;             print-color-adjust: exact;           }        }        @page {           size: A5 landscape;           margin: 4mm;         }      `}</style>      <div style={{        width: '100%',        maxHeight: 'calc(100vh - 8mm)',        border: '1px solid #C69A72',        borderRadius: 8,        padding: '8mm',        pageBreakInside: 'avoid',        display: 'flex',        flexDirection: 'column',        justifyContent: 'space-between',        overflow: 'hidden'      }}>        {/* Header - Compact */}        <div style={{ textAlign: 'center', marginBottom: '6mm', pageBreakInside: 'avoid' }}>          {/* Top-right meta to avoid extra page */}          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: '1mm' }}>            <div />            <div style={{ fontSize: 9, color: '#155446', textAlign: 'right' }}>              فاتورة مستخرجة من نظام قرطبة بتاريخ: {currentDate}            </div>          </div>          <h1 style={{ margin: 0, fontSize: 20, fontWeight: 700 }}>فاتورة</h1>          <p style={{ margin: '4px 0 0', color: '#155446', fontSize: 12 }}>            رقم الفاتورة: <span style={{ fontFamily: 'ui-monospace, SFMono-Regular, Menlo' }}>{invoice.id}</span>          </p>          <div style={{ height: 1, background: 'rgba(198,154,114,0.6)', marginTop: '4mm' }} />        </div>        {/* Main Content - Compact Grid Layout */}        <div style={{           display: 'grid',           gridTemplateColumns: '1fr 1fr',           gap: '4mm',           flex: 1,          alignItems: 'start',          pageBreakInside: 'avoid'        }}>          {/* Left Column */}          <div style={{ display: 'flex', flexDirection: 'column', gap: '3mm' }}>            {/* Customer name */}            <div>              <div style={{ color: '#13312A', marginBottom: 4, fontWeight: 600, fontSize: 12 }}>اسم الزبون:</div>              <div style={{                padding: '6px 8px',                background: '#FDFBF7',                border: '1px solid #C69A72',                borderRadius: 6,                fontSize: 12,                minHeight: 28,                display: 'flex',                alignItems: 'center'              }}>                {invoice.customerName}              </div>            </div>            {/* Payment details */}            <div>              <div style={{ color: '#13312A', marginBottom: 4, fontWeight: 600, fontSize: 12 }}>المبلغ المدفوع:</div>              <div style={{                 padding: '6px 8px',                 background: '#E8F7EE',                 border: '1px solid #98D4B2',                 borderRadius: 6,                 fontSize: 12,                minHeight: 28,                display: 'flex',                alignItems: 'center'              }}>                {formatShortCurrency(invoice.paid || 0)}              </div>            </div>            {/* Remaining amount */}            <div>              <div style={{ color: '#13312A', marginBottom: 4, fontWeight: 600, fontSize: 12 }}>المبلغ المتبقي:</div>              <div style={{                 padding: '6px 8px',                 background: '#FFF4E5',                 border: '1px solid #F59E0B',                 borderRadius: 6,                 fontSize: 12,                minHeight: 28,                display: 'flex',                alignItems: 'center'              }}>                {formatShortCurrency(remaining)}              </div>            </div>            {/* Contact info */}            {invoice.phone ? (              <div>                <div style={{ color: '#13312A', marginBottom: 4, fontWeight: 600, fontSize: 12 }}>الهاتف:</div>                <div style={{                   padding: '6px 8px',                   background: '#FDFBF7',                   border: '1px solid #C69A72',                   borderRadius: 6,                   fontSize: 12,                  minHeight: 28,                  display: 'flex',                  alignItems: 'center'                }}>                  {invoice.phone}                </div>              </div>            ) : null}          </div>          {/* Right Column */}          <div style={{ display: 'flex', flexDirection: 'column', gap: '3mm' }}>            {/* Payment date */}            <div>              <div style={{ color: '#13312A', marginBottom: 4, fontWeight: 600, fontSize: 12 }}>تاريخ الدفع:</div>              <div style={{                 padding: '6px 8px',                 background: '#FDFBF7',                 border: '1px solid #C69A72',                 borderRadius: 6,                 fontSize: 12,                minHeight: 28,                display: 'flex',                alignItems: 'center'              }}>                {invoice.paymentDate ? formatDate(invoice.paymentDate) : '—'}              </div>            </div>            {/* Receive date */}            <div>              <div style={{ color: '#13312A', marginBottom: 4, fontWeight: 600, fontSize: 12 }}>تاريخ الاستلام:</div>              <div style={{                 padding: '6px 8px',                 background: '#FDFBF7',                 border: '1px solid #C69A72',                 borderRadius: 6,                 fontSize: 12,                minHeight: 28,                display: 'flex',                alignItems: 'center'              }}>                {formatDate(invoice.receivedDate)}              </div>            </div>            {/* Delivery date */}            <div>              <div style={{ color: '#13312A', marginBottom: 4, fontWeight: 600, fontSize: 12 }}>تاريخ التسليم:</div>              <div style={{                 padding: '6px 8px',                 background: '#FDFBF7',                 border: '1px solid #C69A72',                 borderRadius: 6,                 fontSize: 12,                minHeight: 28,                display: 'flex',                alignItems: 'center'              }}>                {formatDate(invoice.deliveryDate)}              </div>            </div>            {/* Address */}            {invoice.address ? (              <div>                <div style={{ color: '#13312A', marginBottom: 4, fontWeight: 600, fontSize: 12 }}>العنوان:</div>                <div style={{                   padding: '6px 8px',                   background: '#FDFBF7',                   border: '1px solid #C69A72',                   borderRadius: 6,                   fontSize: 12,                  minHeight: 28,                  display: 'flex',                  alignItems: 'center'                }}>                  {invoice.address}                </div>              </div>            ) : null}          </div>        </div>        {/* Notes - Compact */}        {invoice.notes ? (          <div style={{ marginTop: '4mm', pageBreakInside: 'avoid' }}>            <div style={{ color: '#13312A', marginBottom: 4, fontWeight: 600, fontSize: 12 }}>الملاحظات:</div>            <div style={{               padding: '6px 8px',               background: '#F6E9CA',               border: '1px solid #C69A72',               borderRadius: 6,               fontSize: 12,              minHeight: 28,              display: 'flex',              alignItems: 'center'            }}>              {invoice.notes}            </div>          </div>        ) : null}        {/* Print button (hidden on print) */}        <div style={{           paddingTop: '4mm',           textAlign: 'center',           pageBreakInside: 'avoid'         }}>          <button className="print-btn" onClick={() => window.print()} style={{            background: '#155446',            color: '#F6E9CA',            border: '1px solid #13312A',            borderRadius: 6,            padding: '6px 12px',            cursor: 'pointer',            fontSize: 12,          }}>            طباعة الفاتورة          </button>        </div>      </div>    </div>  );}export default PrintableInvoice;